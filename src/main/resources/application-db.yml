# spring
spring:
  config:
    activate:
      on-profile: dev
  # datasource
  datasource:
    driver-class-name: oracle.jdbc.driver.OracleDriver
    url: jdbc:oracle:thin:@118.33.122.24:15322:KMDBP19
    username: fxs
    password: StgFXS1104#
    hikari:
      minimum-idle: 1
      maximum-pool-size: 50
      max-lifetime: 20000
  # jpa
  jpa:
    database-platform: oracle.jdbc.driver.OracleDriver
    defer-datasource-initialization: true
    show-sql: true
    generate-ddl: true
    open-in-view: false
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        format_sql: true

---

# spring
spring:
  config:
    activate:
      on-profile: test
  # datasource
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:fxs
    username: sa
    password:
    hikari:
      minimum-idle: 1
      maximum-pool-size: 50
      max-lifetime: 20000
  # jpa
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    defer-datasource-initialization: true
    show-sql: true
    generate-ddl: true
    open-in-view: false
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        format_sql: true
  h2:
    console:
      enabled: true
